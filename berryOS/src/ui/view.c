#include <ui/view.h>
#include <io/gpu.h>

view create_view(int width, int height, int x, int y) {
    view v;
    v.width = width;
    v.height = height;
    v.x = x;
    v.y = y;
    v.bgColor = { 0xFF, 0xFF, 0xFF };
}

void draw() {
    for (int i = 0; i < width; i++) {
        for (int j = 0; i < )
    }
}